package com.hci.Pome.entities;

import javax.persistence.*;

//@Entity
//@Table(name = "PhotoTag", schema="pome_data")
public class PhotoTag {
    /*
    @Id
    @Column(name = "PhotoTagID")
    @GeneratedValue(strategy=GenerationType.AUTO)
    private Long photoTagId;

    @Column(name = "PhotoID")
    private Long photoId;

    @Column(name = "TagID")
    private Long tagId;

    protected PhotoTag() {}

    public PhotoTag(Long photoId, Long tagId) {
        this.photoId = photoId;
        this.tagId = tagId;
    }

    @Override
    public String toString() {
        return String.format(
                "PhotoTag[id=%d, photoId='%d', tagId='%d']", photoTagId, photoId, tagId);
    }

    public Long getId() {
        return photoTagId;
    }

    public Long getPhotoId() {
        return photoId;
    }

    public Long getTagId() {
        return tagId;
    }

     */
}